# Web framework
fastapi==0.139.0
uvicorn[standard]==0.50.2
python-multipart==0.0.32

# Auth
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==4.0.1

# Database
psycopg[binary]==3.3.4

# Config / HTTP client
python-dotenv==1.2.2
httpx==0.28.1

# RAG: vector store + embeddings
chromadb==1.5.9
sentence-transformers==5.6.0

# CPU-only PyTorch (used by sentence-transformers).
# For a GPU host, delete the index line below and reinstall torch.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.1
